{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Plan</th>
                <td>{{ salle.Plan }}</td>
            </tr>
            <tr>
                <th>Amphi</th>
                <td>{{ salle.Amphi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>EmplacementPrise</th>
                <td>{{ salle.emplacementPrise }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.Nom }}</td>
            </tr>
            <tr>
                <th>NbPlace</th>
                <td>{{ salle.NbPlace }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'id': salle.id}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
